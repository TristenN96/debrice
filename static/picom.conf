# debrice picom.conf — minimal compositor config.
# Debian 13's picom FATALs on a bare `picom` invocation with
# "Backend not specified. You must choose one explicitly", so the backend
# must be pinned here (the rice autostarts bare `picom` from xprofile).
backend = "glx";
vsync = true;
